The `rhtap-app` is the default deployment namespace prefix. As a cluster administrator can customize the default deployment namespace prefix. To customize the default deployment namespace prefix, refer <tbd>.
