NOTE: In addition to the container image, if there are other configuration changes that you want to promote from development to stage environment, copy those changes from the `deployment-patch.yaml` file in the `development` directory  and paste those in the  `deployment-patch.yaml` in  the  `stage` directory.
