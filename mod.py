.. Log in to the OpenShift web console, in the Administrator perspective, create three namespaces with suffixes `-development` , `-prod`, `-stage`.

.. In each of those namespaces run the `dev_namespace_setup` pipeline that can be found in the `rhtap` namespace.
